//método includes para buscar elementos en un array
let numbers = [1, 3, 7, 8, 3, 12, 1];
let n = 1;
if (numbers.includes(n)) {
  console.log(`The array includes ${n}`);
} else {
  console.log("The array does not include 3");
}

//exponentes
let base = 4;
let exponent = 4;
let result = base ** exponent;

console.log(result);
